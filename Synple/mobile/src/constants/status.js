const STATUS_LABELS = { PENDING: 'Pendente', APPROVED: 'Aprovado', REJECTED: 'Rejeitado', REVOKED: 'Revogado' };
const SUBSYSTEM_LABELS = { ONLINE: 'Operacional', RESTARTING: 'Reiniciando' };

module.exports = { STATUS_LABELS, SUBSYSTEM_LABELS };
